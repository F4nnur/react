import React from 'react';
import {Counter} from "../../components/Counter";

const MainPage = () => {
    return (
        <div>
            Main
            <Counter/>
        </div>
    );
};

export default MainPage;
